# Extra packages the conversion DAG needs on top of the Airflow image.
dbt-postgres>=1.8
feast[postgres,redis]>=0.41
joblib>=1.4
mlflow>=2.16
numpy>=2.0
pandas>=2.2
psycopg2-binary>=2.9
psycopg[binary]>=3.2
pyarrow>=17
scikit-learn>=1.5
# Airflow 2.10 requires SQLAlchemy 1.4; do not upgrade to 2.x in this image.
sqlalchemy>=1.4.36,<2.0
PyYAML>=6.0
